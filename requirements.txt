# LUNA-MATCH AI Layer - Requirements
# ─────────────────────────────────────────────────────────────────────────────
# Install order (recommended):
#   1. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   2. pip install git+https://github.com/huggingface/transformers   # Qwen3-VL requires latest
#   3. pip install -r requirements.txt

# Core & Framework
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
python-multipart>=0.0.9       # Required for FastAPI file uploads

# UI
streamlit>=1.35.0
pillow>=10.3.0

# RAG & Vector Search
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
numpy>=1.26.0
pypdf>=4.2.0
pymupdf>=1.24.0

# Multimodal VLM — Qwen3-VL-8B-Instruct
# NOTE: transformers must be installed from GitHub (Qwen3VLForConditionalGeneration
#       is not yet in the stable PyPI release):
#   pip install git+https://github.com/huggingface/transformers
torch>=2.2.0
torchvision>=0.17.0
accelerate>=0.30.0            # Required for device_map="auto"
qwen-vl-utils>=0.0.8          # Official Qwen-VL preprocessing utilities

# OpenAI-compatible API (vLLM / OpenRouter / self-hosted)
openai>=1.30.0

# Testing
pytest>=8.1.0
pytest-asyncio>=0.23.0
